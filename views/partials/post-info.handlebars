{{!-- <article class="post">
    <div class="title">
        <a>{{title}}</a>
    </div>
    <div class="meta">
        <span>({{ content }})</span>
        By {{user.username}} on {{format_date created_at}}
        |
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article> --}}

<article class="message is-dark">
    <div class="message-header">
        <a href="/post/{{id}}">{{title}}</a>

        <p>
            <a href="/user/{{id}}"><strong>{{user.username}}</strong></a> on <i>{{format_date created_at}}</i>
            |
            <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
        </p>
    </div>

    <div class="message-body" id="main-page-text">
        <span>{{content}}</span>

    </div>
</article>